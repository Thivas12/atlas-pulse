[project]
name = "atlas-pulse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Keerthivasan Kannan" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
atlas-pulse = "atlas_pulse:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
